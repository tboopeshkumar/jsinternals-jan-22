function add(first, second) {
    return first + second;
}

function subtract(first, second) {
    return first - second;
}

function multiply(first, second) {
    return first * second;
}

function divide(first, second) {
    return first / second;
}

var addans = add(10, 20);
if(false) {
    var subans = subtract(10, 20);
}
var multans = multiply(10, 20);
var divans = divide(10, 20);

console.log(addans);    
console.log(subans);
console.log(multans);
console.log(divans);

